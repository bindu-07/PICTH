<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.home.BaseActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.others.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.others.NoInternetActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.auth.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.auth.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.auth.LoginActivity"
            android:exported="true"/>

        <activity
            android:name=".ui.activities.home.MainActivity"
            android:exported="true" />
    </application>

</manifest>